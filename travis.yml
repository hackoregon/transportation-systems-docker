sudo: required
services:
  - docker

before_install:
  - ./bin/build-project.sh -g
script:
  - docker images

after_success:
  - if [ "$TRAVIS_BRANCH" == "master" ]; then
    docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD";
    docker push $DOCKER_USERNAME/$DOCKER_IMAGE;
    fi
